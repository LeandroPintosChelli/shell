#!/usr/bin/env bash
#
File=/var/run/my_process.pid
man=./manage_my_process
if [ "$1" == "start" ];
then
      	$man &
 echo $$ > $File
 echo "manage_my_process started"

elif [ "$1" == "stop" ];
then
	 pkill -f $man
	 sudo rm $File
	 echo "manage_my_process stopped"

 elif [ "$1" == "restart" ];
then
	   pkill -f $man
	   sudo rm $File
	   $man &
	   echo $$ > $File
	   echo "manage_my_process restarted"
 else
	   echo "Usage: manage_my_process {start|stop|restart}"

fi	   
